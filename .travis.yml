language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t tmartinez30052/reactapp -f Dockerfile.dev .

script:
  - docker run -e CI=true tmartinez30052/reacctapp npm run test